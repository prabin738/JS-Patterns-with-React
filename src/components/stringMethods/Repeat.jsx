import React from "react";

const Repeat = () => {
  // Task 1) Use repeat to create a divider of 30 "=" characters.
  const divider = "=".repeat(30);
  alert(divider);
  return <div>Repeat</div>;
};

export default Repeat;
