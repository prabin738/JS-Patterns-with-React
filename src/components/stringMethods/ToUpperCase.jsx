import React from "react";

const ToUpperCase = () => {
  // 6) Use toUpperCase to shout "keep learning".
  const word = "keep learning";
  const finalWord = word.toUpperCase();
  // alert(finalWord);
  console.log(finalWord);
  return <div>ToUpperCase</div>;
};

export default ToUpperCase;
