import React from "react";

const Trim = () => {
  // 6. trim()

  // Challenge: Check if user input is empty after trimming.

  // Input: " "

  // Output: false

  // Hint: Trim first, then check length.

  return <div>Trim</div>;
};

export default Trim;
